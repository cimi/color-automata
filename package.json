{
  "name": "color-automata",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "dat.gui": "^0.7.0",
    "stats.js": "^0.17.0"
  },
  "devDependencies": {
    "copy-webpack-plugin": "^6.0.2",
    "gh-pages": "^1.0.0",
    "husky": "^0.14.3",
    "lint-staged": "^5.0.0",
    "prettier": "^2.0.5",
    "react-scripts": "3.4.1"
  },
  "homepage": "https://cimi.github.io/color-automata",
  "lint-staged": {
    "*.{js,json,css}": [
      "prettier --write",
      "git add"
    ]
  },
  "scripts": {
    "precommit": "lint-staged",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "eject": "react-scripts eject",
    "deploy": "npm run build && node deploy.js"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
